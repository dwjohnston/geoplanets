import { RANGE } from "./ValueTypes";

export default () => ([{
    min: 0,
    max: 100,
    step: 1,
    randMin: 10,
    randMax: 20,
    id: "linkRate",
    label: "Link Rate",
    type: RANGE,
}])