import { RANGE } from "./ValueTypes";

export default () => ([{
    min: 1,
    max: 100,
    step: 1,
    randMin: 10,
    randMax: 20,
    id: "pulseRate",
    label: "PulseRate",
    type: RANGE,

}])